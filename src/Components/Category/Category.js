import Options from './Options/Options';
import './Category.css';

const Category = (props) => {
    return (
        <section>
            Category
            <Options />
        </section>
    )
}

export default Category;