import Stripe from './Stripe/Stripe';
import './About.css';

const About = (props) => {
    return (
        <section>
            About Page
            <Stripe />
        </section>
    )
}

export default About;