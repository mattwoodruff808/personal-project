import './Stripe.css';

const Stripe = () => {
    return (
        <section>
            This component will be implemented later on. Not included in the MVP.
        </section>
    )
}

export default Stripe;